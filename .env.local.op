# Committed template — op:// references only (no plaintext secrets).
# Use: op run --env-file=.env.local.op -- <command>
# Or: pnpm sync:github (scripts/with-op-env.sh picks this up when `op` is installed).

INGESTION_URL=http://127.0.0.1:8081

# 1Password item "gh millipede PAT token" (title has trailing space in vault; UUID path is stable)
GITHUB_TOKEN=op://7matojlodr3s47ndizgkowy5vm/dof6u6q64kc7bmq5hifwcoxwna/credential

# GitLab — replace with op:// when you add a millipede GitLab item
# GITLAB_TOKEN=op://7matojlodr3s47ndizgkowy5vm/<item-id-or-title>/credential
GITLAB_HOST=https://gitlab.com

# Optional: connector watch interval (minutes)
# CONNECTOR_POLL_MINUTES=5
